import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lxdeveloper.cuestionario',
  appName: 'cuestionario',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
